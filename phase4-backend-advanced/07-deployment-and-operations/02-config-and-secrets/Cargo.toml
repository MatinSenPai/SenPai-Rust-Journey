[package]
name = "p4-07-02-config-and-secrets"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

# Deliberately no dependencies at all — the whole point of this lesson is
# that a layered, fail-fast config loader is ~60 lines of stdlib.
[dependencies]
