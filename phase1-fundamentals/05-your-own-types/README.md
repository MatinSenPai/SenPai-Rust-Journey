# 05 — Your own types

So far you have used the types the language gave you. This module is about
making your own, and about the pattern matching that makes them worth having:
a `match` the compiler checks for exhaustiveness, so that adding a case to an
enum turns every place needing an update into a compile error.

1. [Structs and methods](01-structs-and-methods/README.md)
2. [Tuple structs and the newtype pattern](02-tuple-structs-and-newtype/README.md)
3. [Enums as data](03-enums-as-data/README.md)
4. [`match` in depth](04-match-in-depth/README.md)
5. [`if let`, `while let`, `let else`](05-if-let-while-let-let-else/README.md)
