[package]
name = "p1-02-03-clone-and-copy"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

# `04-copy-needs-no-heap` and `05-cloning-a-reference` do not compile. They are
# the subject of "Errors you will meet", so they sit behind a feature and the
# repository stays green.
[features]
broken = []

[[example]]
name = "04-copy-needs-no-heap"
required-features = ["broken"]

[[example]]
name = "05-cloning-a-reference"
required-features = ["broken"]
