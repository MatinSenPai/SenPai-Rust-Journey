[package]
name = "p1-06-02-option-combinators"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

# `05-map-instead-of-and-then` and `06-map-moves-the-option` do not compile.
# They are the subject of "Errors you will meet", so they sit behind a
# feature and the repository stays green.
[features]
broken = []

[[example]]
name = "05-map-instead-of-and-then"
required-features = ["broken"]

[[example]]
name = "06-map-moves-the-option"
required-features = ["broken"]
