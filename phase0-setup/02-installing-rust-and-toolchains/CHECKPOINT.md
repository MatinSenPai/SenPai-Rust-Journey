# Checkpoint — Installing Rust and toolchains

1. What does `rustup` manage that `rustc` alone does not? Name the closest
   tool you know from the Python world.
2. What does this repo's `rust-toolchain.toml` guarantee, and why is that
   useful for a project you might come back to after months away?
3. What problem do Rust "editions" solve, and how is that different from how
   Python 2 → 3 was handled?
4. Run `rustc --version` and `cargo --version` right now and paste the output
   somewhere you'll see it (a scratch note is fine) — what version did you
   get, and does it match "stable" as of today?
