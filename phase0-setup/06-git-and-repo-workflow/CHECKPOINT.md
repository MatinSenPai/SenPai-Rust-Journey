# Checkpoint — Git and repo workflow

1. What three files/locations does this repo rely on so that you (or anyone
   else) can tell "where am I in this journey" without asking anyone?
2. Write out (don't run yet) the exact commit message you'd use after
   finishing `phase1-fundamentals/01-variables-types-control-flow/01-variables-mutability-shadowing`.
3. Why does the convention say to flip the `PROGRESS.md` checkbox in the
   *same* commit as the lesson, rather than batching several checkbox
   updates into one later commit?
4. Find the comment in the root `Cargo.toml` that lists which lessons are
   intentionally left out of `members`. Why would including them break
   `cargo build --workspace`?
