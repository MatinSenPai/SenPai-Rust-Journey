[package]
name = "p3-01-03-http-semantics-you-must-know"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

# `06` fails to compile, `07` panics. Both sit behind a feature so running
# them is a deliberate act and the workspace stays green by default — see
# docs/lesson-standard.md §5.
[features]
broken = []

[[example]]
name = "06-status-class-non-exhaustive-broken"
required-features = ["broken"]

[[example]]
name = "07-accept-q-value-panic-broken"
required-features = ["broken"]
