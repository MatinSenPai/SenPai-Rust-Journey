[package]
name = "p3-01-01-tcp-echo-server"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

[dependencies]

# `02` does not compile — that's the point (see `## Errors you will meet`).
# It sits behind a feature so the workspace stays green by default and
# running it is a deliberate act.
[features]
broken = []

[[example]]
name = "02-double-mut-borrow-broken"
required-features = ["broken"]
