[package]
name = "p2-05-01-custom-error-types"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

# `06` and `07` do not compile. Both are the subject of "Errors you will
# meet", so they sit behind a feature and the repository stays green.
[features]
broken = []

[[example]]
name = "06-error-needs-debug-and-display"
required-features = ["broken"]

[[example]]
name = "07-source-needs-the-trait-in-scope"
required-features = ["broken"]
