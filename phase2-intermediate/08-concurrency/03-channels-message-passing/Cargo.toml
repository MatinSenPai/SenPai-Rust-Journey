[package]
name = "p2-08-03-channels-message-passing"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

# `07` panics, `08` fails to compile. Both sit behind a feature so running
# them is a deliberate act and the workspace stays green by default — see
# docs/lesson-standard.md §5.
[features]
broken = []

[[example]]
name = "07-recv-on-disconnected-channel-broken"
required-features = ["broken"]

[[example]]
name = "08-use-after-send-broken"
required-features = ["broken"]
