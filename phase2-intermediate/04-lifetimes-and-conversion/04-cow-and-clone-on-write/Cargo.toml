[package]
name = "p2-04-04-cow-and-clone-on-write"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

# `06`-`08` do not compile. Each sits behind a feature so running it is a
# deliberate act and the workspace stays green by default — see
# docs/lesson-standard.md §5.
[features]
broken = []

[[example]]
name = "06-forgot-to-wrap-in-cow-broken"
required-features = ["broken"]

[[example]]
name = "07-owned-wants-string-broken"
required-features = ["broken"]

[[example]]
name = "08-to-mut-borrow-conflict-broken"
required-features = ["broken"]
