[package]
name = "p2-02-05-laziness-and-performance"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

# `06` and `07` fail to compile. Both sit behind a feature so running them is
# a deliberate act and the workspace stays green by default — see
# docs/lesson-standard.md §5.
[features]
broken = []

[[example]]
name = "06-len-on-infinite-iterator"
required-features = ["broken"]

[[example]]
name = "07-cycle-needs-clone"
required-features = ["broken"]
