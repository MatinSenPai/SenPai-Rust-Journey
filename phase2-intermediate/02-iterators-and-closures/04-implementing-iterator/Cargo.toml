[package]
name = "p2-02-04-implementing-iterator"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

# `06`-`09` do not compile. Each sits behind a feature so running it is a
# deliberate act and the workspace stays green by default — see
# docs/lesson-standard.md §5.
[features]
broken = []

[[example]]
name = "06-missing-next-method"
required-features = ["broken"]

[[example]]
name = "07-next-wrong-self-mutability"
required-features = ["broken"]

[[example]]
name = "08-use-after-move"
required-features = ["broken"]

[[example]]
name = "09-reference-not-into-iterator"
required-features = ["broken"]
