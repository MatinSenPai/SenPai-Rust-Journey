[package]
name = "p2-03-05-associated-types"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

# `05`-`07` do not compile. Each sits behind a feature so running it is a
# deliberate act and the workspace stays green by default — see
# docs/lesson-standard.md §5.
[features]
broken = []

[[example]]
name = "05-two-impls-conflict"
required-features = ["broken"]

[[example]]
name = "06-ambiguous-convert-call"
required-features = ["broken"]

[[example]]
name = "07-mismatched-target-type"
required-features = ["broken"]

[[example]]
name = "08-same-target-twice-conflicts"
required-features = ["broken"]
