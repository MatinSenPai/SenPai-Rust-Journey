[package]
name = "p2-03-07-static-vs-dynamic-dispatch"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

# `06`-`10` do not compile. Each sits behind a feature so running it is a
# deliberate act and the workspace stays green by default — see
# docs/lesson-standard.md §5.
[features]
broken = []

[[example]]
name = "06-vec-dyn-no-box-broken"
required-features = ["broken"]

[[example]]
name = "07-missing-dyn-keyword-broken"
required-features = ["broken"]

[[example]]
name = "08-not-object-safe-self-by-value-broken"
required-features = ["broken"]

[[example]]
name = "09-not-object-safe-generic-method-broken"
required-features = ["broken"]

[[example]]
name = "10-branch-mismatch-broken"
required-features = ["broken"]
