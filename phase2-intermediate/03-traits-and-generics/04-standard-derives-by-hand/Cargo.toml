[package]
name = "p2-03-04-standard-derives-by-hand"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

# `09` and `10` fail to compile. Both sit behind a feature so running them is
# a deliberate act and the workspace stays green by default — see
# docs/lesson-standard.md §5.
[features]
broken = []

[[example]]
name = "09-derive-eq-needs-field-eq"
required-features = ["broken"]

[[example]]
name = "10-hashset-needs-hash-and-eq"
required-features = ["broken"]
