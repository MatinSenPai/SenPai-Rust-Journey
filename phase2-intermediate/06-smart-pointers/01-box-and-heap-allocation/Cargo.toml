[package]
name = "p2-06-01-box-and-heap-allocation"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

# `05` and `06` do not compile. Each sits behind a feature so running it is a
# deliberate act and the workspace stays green by default — see
# docs/lesson-standard.md §5.
[features]
broken = []

[[example]]
name = "05-box-str-from-literal-broken"
required-features = ["broken"]

[[example]]
name = "06-move-out-through-shared-ref-broken"
required-features = ["broken"]
