[package]
name = "p2-06-02-recursive-types-and-trait-objects"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

# `01` and `04` do not compile. Each sits behind a feature so running it is a
# deliberate act and the workspace stays green by default — see
# docs/lesson-standard.md §5.
[features]
broken = []

[[example]]
name = "01-naive-expr-broken"
required-features = ["broken"]

[[example]]
name = "02-boxed-expr-eval"

[[example]]
name = "03-size-stays-fixed"

[[example]]
name = "04-half-boxed-broken"
required-features = ["broken"]

[[example]]
name = "05-vec-box-dyn-shape"
